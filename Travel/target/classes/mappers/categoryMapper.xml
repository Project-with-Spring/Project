<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.Travel.mapper.CategoryMapper">
	<select id="selectCategoryList" resultType="category">
		SELECT * FROM category
	</select>
	
	<select id="selectCategory" resultType="category">
		SELECT * FROM category WHERE ctg_id=#{ctg_id }
	</select>
	
	<select id="selectCtgPdtList" resultType="category">
		select c.*, p.*
		from (
			select row_number() over(order by pdt_id) as num, pdt.*
			from product pdt
				) p join category c on c.ctg_id = p.ctg_id
		where num between #{start} AND #{start+19}
		order by p.pdt_id;
	</select>
	
</mapper>
